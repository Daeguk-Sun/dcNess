# Candidate

`ExportReceiver.onReceive()`는 repository의 일반 함수 호출 검색에서 caller가 0개다. 자동 dead-code scanner는 class 전체 삭제를 제안했다.

Cartography에는 `export-receiver: landed`, owner `attachments`, runtime entrypoint `ACTION_EXPORT`가 기록돼 있다.
