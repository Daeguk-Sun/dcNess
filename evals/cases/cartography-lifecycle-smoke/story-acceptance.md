# Story acceptance contract

- AC-2101 [command]: exporting an existing attachment through the CLI exits 0 and writes identical bytes.
- AC-2102 [agent-read]: the landed CLI route keeps ExportService as owner and preserves storage policy and ADR-0021.
